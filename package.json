{
  "name": "redux-saga-caller",
  "version": "1.0.0",
  "description": "Prevent calling redux sagas twice with same params in paralel.",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src/index.js -o dist/index.js",
    "test": "babel-node src/index.test.js"
  },
  "author": "Juraj Husar",
  "license": "ISC",
  "peerDependencies": {
    "redux-saga": "*"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "redux-saga": "^1.0.0"
  }
}
